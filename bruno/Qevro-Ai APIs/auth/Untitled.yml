info:
  name: Untitled
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/api/register
  body:
    type: json
    data: |-
      {
        "username": "prince",
        "email": "princebhari203@gmail.com",
        "password": "prince123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
