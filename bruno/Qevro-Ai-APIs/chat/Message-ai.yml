info:
  name: Message-ai
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/api/message
  body:
    type: json
    data: |-
      {
         "message": "Top 5 programming languages to learn in 2026 that will be in demand in the future."
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
